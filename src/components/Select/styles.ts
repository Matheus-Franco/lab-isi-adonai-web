import styled from 'styled-components';

export const Container = styled.div`
    select {
        padding: 16px;
        margin-right: 24px;
        border-radius: 4px;
        background: #fff;
        border-color: #ff9000;
        color: #ff9000;
    }
`;
